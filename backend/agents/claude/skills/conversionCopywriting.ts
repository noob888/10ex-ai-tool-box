import type { Skill } from '../types';

export const conversionCopywritingSkill: Skill = {
  id: 'conversion_copywriting',
  name: 'Conversion Copywriting',
  prompt:
    'Use proven frameworks (PAS, AIDA, value + proof + CTA). Lead with relevance, then a clear benefit, then one concrete proof point, then a low-friction CTA.',
};

