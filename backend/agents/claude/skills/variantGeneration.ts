import type { Skill } from '../types';

export const variantGenerationSkill: Skill = {
  id: 'variant_generation',
  name: 'Variant Generation',
  prompt:
    'Generate multiple options with distinct angles (different hooks, different proof types, different CTAs) while staying consistent with the inputs.',
};

