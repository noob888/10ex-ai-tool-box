import type { Skill } from '../types';

export const toneControlSkill: Skill = {
  id: 'tone_control',
  name: 'Tone Control',
  prompt:
    'Maintain the requested tone consistently. Keep sentences tight, confident, and human. Avoid hype, buzzwords, and overly formal phrasing unless explicitly requested.',
};

